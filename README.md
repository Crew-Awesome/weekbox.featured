# WeekBox Featured

Published GameBanana rankings for WeekBox. `public/featured.json` includes all
supported engines and `public/featured-vslice.json` includes only V-Slice
submissions. The generator preserves the ranks from GameBanana's
`Game/8694/TopSubs` feed first, then fills its five slots with whitelisted,
non-excluded submissions using a single popularity order.
