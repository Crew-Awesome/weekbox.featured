# WeekBox Featured

Published GameBanana rankings for WeekBox. The generator preserves the ranks
from GameBanana's `Game/8694/TopSubs` feed, then removes only submissions that
fall outside the supported category-root whitelist or appear in its explicit
exclusion list.
